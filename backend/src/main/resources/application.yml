server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-wispy-heart-ahwgglaz-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:npg_iSOaxk1TYZp8}
    driver-class-name: org.postgresql.Driver

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:soporteamazzonas@gmail.com}
    password: ${MAIL_PASSWORD:vneqickvtjoxboto}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

n8n:
  webhook-url: ${N8N_ALERT_WEBHOOK_URL:http://n8n:5678/webhook/alerta-padres}
  test-email: ${TEST_EMAIL_RECIPIENT:dayro100806@gmail.com}
  entorno: ${ENTORNO:desarrollo}


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: cGFydGFsYWNhZGVtaWNvU2FuQWd1c3RpbkNhbXB1czIwMjVTZWN1cmVLZXk=
  expiration: 86400000

openai:
  api:
    key: ${GEMINI_API_KEY:${OPENAI_API_KEY:}}
    url: "https://generativelanguage.googleapis.com/v1beta/openai/chat/completions"
    model: "gemini-2.5-flash"

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    metrics:
      enabled: true
    health:
      show-details: always


